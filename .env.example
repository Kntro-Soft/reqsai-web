# Copy to .env and adjust. `.env` is git-ignored.
# Used by Docker Compose for variable interpolation.
# The Angular dev server uses proxy.conf.json for API routing — no build-time env vars needed.

# Port exposed by the nginx container (production / compose)
APP_PORT=4200

# Angular dev server port (bun run start)
# DEV_PORT=4200
